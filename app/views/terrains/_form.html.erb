<%= form_for @terrain do |f| %>
    <% if @terrain.errors.any? %>
        <div>
            <h2>
                <%= pluralize(@terrain.errors.count, "error") %> prohibited this terrain from being saved:
            </h2>
            <ul>
                <% @terrain.errors.full_messages.each do |msg| %>
                    <li><%= msg %></li>
                <% end %>
            </ul>
        </div>
    <% end %>
    
    <p>
        <%= f.label :name %><br>
        <%= f.text_field :name %>
    </p>
    
    <p>
        <%= f.submit %>
    </p>

<% end %>